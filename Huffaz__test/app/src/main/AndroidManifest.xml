<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Huffaz__test"
        tools:targetApi="31">
        <activity
            android:name=".huffazai.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".huffazai.AccountActivity"
            android:exported="true">
            <!-- Intent filters if needed -->
        </activity>

        <activity
            android:name=".huffazai.MainActivity"
            android:exported="true">
            <!-- Intent filters if needed -->
        </activity>

        <activity
            android:name=".huffazai.ResetPasswordActivity"
            android:exported="true">
            <!-- Intent filters if needed -->
        </activity>

        <activity
            android:name=".huffazai.SignupActivity"
            android:exported="true">
            <!-- Intent filters if needed -->
        </activity>

        <!-- Declare the SurahDetailActivity -->
        <activity
            android:name=".activities.SurahDetailActivity"
            android:label="SurahDetailActivity">
        </activity>
    </application>
</manifest>
